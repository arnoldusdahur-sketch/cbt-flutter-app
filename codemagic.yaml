workflows:
  ios-simulator-build:
    name: Flutter iOS Simulator for Appetize
    max_build_duration: 60
    instance_type: mac_mini_m1
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Flutter Packages Get
        script: |
          flutter packages pub get
      - name: Build iOS Simulator
        script: |
          # Build simulator .app tanpa sertifikat developer
          flutter build ios --simulator --no-codesign
          # Masuk ke folder output build simulator
          cd build/ios/iphonesimulator
          # Kompres Runner.app menjadi Runner.zip agar dikenali Appetize
          zip -r Runner.zip Runner.app
    artifacts:
      - build/ios/iphonesimulator/Runner.zip
    publishing:
      scripts:
        - name: Upload to Appetize
          script: |
            curl --location --request POST "https://api.appetize.io/v1/apps" \
              -H "X-API-KEY: tok_wzmvs2gv5gp557fwnrpp6kv644" \
              --form "file=@build/ios/iphonesimulator/Runner.zip" \
              --form "platform=ios"
